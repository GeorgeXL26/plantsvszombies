# Plants vs Zombies

A single-file offline HTML build of Plants vs Zombies.

## Usage

Download `plantsvszombies.html` and open it directly in a browser (`file://`) — no internet connection or build step required.
